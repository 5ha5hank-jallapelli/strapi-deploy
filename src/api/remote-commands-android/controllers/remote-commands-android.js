'use strict';

/**
 * remote-commands-android controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::remote-commands-android.remote-commands-android');
